# Empty on purpose -- this file's only job is to mark "strategies" as a
# Python package (a folder Python will treat as importable), the same role
# an empty file plays when a C# project just needs a namespace to exist.
